<html><head><style>body{font-family:'Segoe UI', Calibri, sans-serif; line-height:1.6;} h1{color:#2E4053; border-bottom:2px solid #2E4053;} h2{color:#2874A6;} code{background:#F4F6F6; padding:2px; font-family:Consolas, monospace;}</style></head><body><h1>HTML Template: pending_tutors.html</h1>
<h2>1. Executive Summary</h2>
<p>Defines the structural layout and content for a specific web page view.</p>
<h2>2. Code Logic &amp; Functionality</h2>
<p>Uses the Jinja2 templating engine. The file allows for dynamic data insertion using <code>{{ variable }}</code> syntax and control structures like <code>{% if %}</code> loops within standard HTML markup.</p>
<h2>3. Key Concepts &amp; Definitions</h2>
<ul>
<li><strong>Jinja2</strong>: A modern and designer-friendly templating language for Python.</li>
<li><strong>Template Inheritance</strong>: The ability to extend a base layout (<code>base.html</code>) to avoid code duplication.</li>
<li><strong>DOM</strong>: Document Object Model, the data representation of the objects that comprise the structure and content of a document on the web.</li>
</ul>
<h2>4. Location Details</h2>
<p><strong>Path</strong>: <code>templates\admin\pending_tutors.html</code>
<strong>Type</strong>: .HTML File</p>
<h2>5. Source Code Preview (Snippet)</h2>
<p>Running typical software analysis on this file:</p>
<p>```html
{% extends 'base.html' %}</p>
<p>{% block title %}Pending Tutor Applications - Admin{% endblock %}</p>
<p>{% block content %}</p>
<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h2 class="mb-4"><i class="fas fa-users"></i> Tutor Applications Management</h2>

            <!-- Tabs -->
            <ul class="nav nav-tabs mb-4" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" data-bs-toggle="tab" href="#pending">
                        <i class="fas fa-clock"></i> Pending
                        {% if pending|length > 0 %}
                        <span class="badge bg-warning">{{ pending|length }}</span>
                        {% endif %}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" href="#approved">
                        <i class="fas fa-check-circle"></i> Approved ({{ approved|length }})
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" href="#rejected">
                        <i class="fas fa-times-circle"></i> Rejected ({{ rejected|length }})
                    </a>
                </li>
            </ul>

            <!-- Tab Content -->
            <div class="tab-content">
                <!-- Pending Tab -->
                <div id="pending" class="tab-pane active">
                    <h4 class="mb-3">Pending Applications</h4>

                    {% if pending|length == 0 %}
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i> No pending applications
                    </div>
                    {% else %}
                    <div class="row">
                        {% for tutor in pending %}
                        <div class="col-md-6 mb-4">
                            <div class="card shadow-sm">
                                <div class="card-header bg-warning text-dark">
                                    <h5 class="mb-0"><i class="fas fa-user"></i> {{ tutor.full_name }}</h5>
                                </div>
                                <div class="card-body">
                                    <p><strong><i class="fas fa-envelope"></i> Email:</strong> {{ tutor.email }}</p>
                                    <p><strong><i class="fas fa-star"></i> Specialization:</strong> {{
                                        tutor.specialization }}</p>
                                    <p><strong><i class="fas fa-graduation-cap"></i> Qualifications:</strong></p>
                                    <div class="border-start border-primary ps-3 mb-3"
                                        style="max-height: 100px; overflow-y: auto;">
                                        {{ tutor.qualifications }}
                                    </div>

                                    {% if tutor.bio %}
                                    <p><strong><i class="fas fa-info-circle"></i> Bio:</strong></p>
                                    <div class="border-start border-secondary ps-3 mb-3"
                                        style="max-height: 100px; overflow-y: auto;">
                                        {{ tutor.bio }}
                                    </div>
                                    {% endif %}

                                    <p class="text-muted small">
                                        <i class="fas fa-calendar"></i> Applied: {{ tutor.application_date.strftime('%B
                                        %d, %Y %I:%M %p') }}
                                    </p>

                                    <div class="d-grid gap-2">
                                        <form method="POST" action="{{ url_for('approve_tutor', tutor_id=tutor.id) }}"
                                            class="d-inline">
                                            <input type="hidden" name="csrf_token" value="{{ csrf_token() }}" />
                                            <button type="submit" class="btn btn-success w-100"
                                                onclick="return confirm('Approve {{ tutor.full_name }}?')">
                                                <i class="fas fa-check"></i> Approve
                                            </button>
                                        </form>
                                        <form method="POST" action="{{ url_for('reject_tutor', tutor_id=tutor.id) }}"
                                            class="d-inline">
                                            <input type="hidden" name="csrf_token" value="{{ csrf_token() }}" />
                                            <button type="submit" class="btn btn-danger w-100"
                                                onclick="return confirm('Reject {{ tutor.full_name }}?')">
                                                <i class="fas fa-times"></i> Reject
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>

                <!-- Approved Tab -->
                <div id="approved" class="tab-pane fade">
                    <h4 class="mb-3">Approved Tutors</h4>

                    {% if approved|length == 0 %}
                    <div class="alert alert-info">No approved tutors</div>
                    {% else %}
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Specialization</th>
                                    <th>Approval Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for tutor in approved %}
                                <tr>
                                    <td><i class="fas fa-check-circle text-success"></i> {{ tutor.full_name }}</td>
                                    <td>{{ tutor.email }}</td>
                                    <td>{{ tutor.specialization }}</td>
                                    <td>
                                        {% if tutor.approval_date %}
                                        {{ tutor.approval_date.strftime('%B %d, %Y') }}
                                        {% else %}
                                        N/A
                                        {% endif %}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    {% endif %}
                </div>

                <!-- Rejected Tab -->
                <div id="rejected" class="tab-pane fade">
                    <h4 class="mb-3">Rejected Applications</h4>

                    {% if rejected|length == 0 %}
                    <div class="alert alert-info">No rejected applications</div>
                    {% else %}
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Specialization</th>
                                    <th>Rejection Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for tutor in rejected %}
                                <tr>
                                    <td><i class="fas fa-times-circle text-danger"></i> {{ tutor.full_name }}</td>
                                    <td>{{ tutor.email }}</td>
                                    <td>{{ tutor.specialization }}</td>
                                    <td>
                                        {% if tutor.approval_date %}
                                        {{ tutor.approval_date.strftime('%B %d, %Y') }}
                                        {% else %}
                                        N/A
                                        {% endif %}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    {% endif %}
                </div>
            </div>

            <div class="mt-4">
                <a href="{{ url_for('admin_dashboard') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to Dashboard
                </a>
            </div>
        </div>
    </div>
</div>
<p>{% endblock %}
```</p></body></html>